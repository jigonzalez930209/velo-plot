# SolidJS integration

```bash
npm install velo-plot solid-js
```

```tsx
import { VeloPlot } from 'velo-plot/solid';

<VeloPlot series={[{ id: 'a', x, y }]} height={400} />
```

Hooks: `useVeloPlot`, `useStackedPlot`, `useIndicator`, `useChartSync` from `velo-plot/solid`.
